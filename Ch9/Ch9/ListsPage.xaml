<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="Lists"
             x:Class="Ch9.ListsPage">
    <ContentPage.Content>

        <StackLayout Orientation="Vertical" 
                     VerticalOptions="StartAndExpand"
                     Padding="5,0,5,5">

            <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand" BackgroundColor="Accent">
                <Label Text="Select list " VerticalTextAlignment="Center"/>
                
                <Picker x:Name="viewedListPicker"
                                    ItemsSource="{Binding MovieLists, Mode=TwoWay}"
                                    ItemDisplayBinding="{Binding Name}"
                                    SelectedItem="{Binding SelectedList}"
                                    HorizontalOptions="FillAndExpand"
                                    BackgroundColor="Beige"/>
            </StackLayout>


            <ListView HasUnevenRows="True"
                      HorizontalOptions="Start"
                      VerticalOptions="StartAndExpand"
                      BackgroundColor="Aqua"
                      ItemsSource="{Binding Source={x:Reference viewedListPicker}, Path=SelectedItem.Movies}">

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal" Padding="5">

                                <Image>
                                    <Image.Source>
                                        <UriImageSource Uri="{Binding ImgSmSrc}"
                                                    CacheValidity="0"
                                                    CachingEnabled="False"                                                    
                                                    />
                                    </Image.Source>
                                </Image>

                                <StackLayout HorizontalOptions="StartAndExpand" Orientation="Vertical">
                                    <Label Text="{Binding Title}" TextColor="Black"/>
                                    <StackLayout Orientation="Horizontal">
                                        <Label TextColor="Gray" Text="{Binding Year}" FontSize="Micro"/>
                                        <Label TextColor="Gray" Text="{Binding Genre}" FontSize="Micro"/>
                                    </StackLayout>
                                </StackLayout>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>            

        </StackLayout>
        
    </ContentPage.Content>
</ContentPage>